define("ZSYWEB.Widget.Btn", ['ZSYWEB.Event'], function () {
    var $core = ZSYWEB.Core;
    $core.createPlug({
        type : "csn-btn",
        templateUrl : "btn.htm",
        template : null,
        render : function( dom, obj ) {
            
        }
    });
});